CREATE TABLE IF NOT EXISTS Ticket (id INTEGER IDENTITY PRIMARY KEY, title VARCHAR(100) NOT NULL);
CREATE TABLE IF NOT EXISTS Comment (id INTEGER IDENTITY PRIMARY KEY, ticket INTEGER NOT NULL, content VARCHAR(1000) NOT NULL);